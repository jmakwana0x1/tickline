/**
 * Load and adversarial campaigns (Phase 7).
 *
 * Ramps a load agent to sustained fills and records p50/p95/p99 fill latency, fills per
 * onchain transaction, gas per 1,000 fills, and ledger write throughput, then asserts
 * **zero drift** at the end (I13). A load test that does not check drift measures only how
 * fast the system can be wrong.
 */

console.error('load harness lands in Phase 7 (PHASES.md). Nothing to run yet.');
process.exit(1);
