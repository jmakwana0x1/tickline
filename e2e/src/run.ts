/**
 * Scenario runner entry point.
 *
 * Phase 6 implements this: boot anvil, deploy contracts, run migrations, start the engine,
 * execute the scenario, assert final balances, tear down. Phase 0 leaves it explicitly
 * unimplemented — a runner that silently passes with no scenarios would be worse than none.
 */

console.error('e2e harness lands in Phase 6 (PHASES.md). Nothing to run yet.');
process.exit(1);
