# contracts/src

Empty until Phase 3. `TicklineVault` lands here, along with the vendored batch-settlement
escrow (pinned by commit, per PHASES.md phase 3).

Nothing in this directory is written before `docs/spec-notes.md` answers what the escrow's
interface actually is. Guessing at the wire format is banned (CLAUDE.md section 2).
