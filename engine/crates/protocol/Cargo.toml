[package]
name = "protocol"
description = "x402 envelopes, voucher and receipt types, EIP-712 hashing. Zero IO."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
thiserror.workspace = true
serde.workspace = true
# ADR-0011. keccak256 needs a backend, and tiny-keccak is the pure-Rust one.
alloy-primitives = { workspace = true, features = ["tiny-keccak"] }
k256.workspace = true
# The 402 envelopes are JSON in a base64 header (#66).
serde_json.workspace = true
base64.workspace = true

[dev-dependencies]
proptest.workspace = true

[lints]
workspace = true
