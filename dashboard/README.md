# dashboard

Phase 8. A read-only Next.js view: market list, price curve, fills-versus-onchain-transactions
counter, creator cost against the subsidy cap, and links to explorer transactions.

Read-only is a deliberate constraint. The dashboard has no keys and can move no money, so it
stays outside the trust story entirely.
